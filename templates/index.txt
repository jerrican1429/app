<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>質數判斷器</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="card mt-5">
            <div class="card-header text-center">
                <h2>質數判斷器</h2>
            </div>
            <div class="card-body">
                <form id="primeForm" class="mb-4">
                    <div class="form-group mb-3">
                        <label for="number" class="form-label">請輸入一個正整數：</label>
                        <input type="number" class="form-control" id="number" required min="1">
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">檢查</button>
                    </div>
                </form>
                <div id="result" class="alert d-none" role="alert"></div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('primeForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const number = document.getElementById('number').value;
            const result = document.getElementById('result');
            
            // 發送請求到伺服器
            fetch('/check_prime', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: `number=${number}`
            })
            .then(response => response.json())
            .then(data => {
                result.classList.remove('d-none');
                
                if (data.success) {
                    result.className = `alert ${data.is_prime ? 'alert-success' : 'alert-info'}`;
                    result.textContent = data.message;
                } else {
                    result.className = 'alert alert-danger';
                    result.textContent = data.message;
                }
            })
            .catch(error => {
                result.classList.remove('d-none');
                result.className = 'alert alert